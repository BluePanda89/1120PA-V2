---
import { navLinks } from '@data/navigation';

const officePhone = "03-7660 4752";
const location = "PJ South, Taman Petaling Utama";
---

<header class="fixed top-0 left-0 w-full z-50">
  <div class="bg-blue-900 text-white py-1.5 px-4 text-xs">
    <div class="container mx-auto flex justify-between items-center">
      <div class="flex gap-4">
        <span>📍 {location}</span>
        <span class="hidden md:inline font-medium">📞 {officePhone}</span>
      </div>
      <div class="text-[10px] uppercase tracking-widest opacity-80">
        1120 Park Avenue JMB Official Portal
      </div>
    </div>
  </div>

  <nav class="bg-white/80 backdrop-blur-md border-b border-slate-200">
    <div class="container mx-auto px-4 h-16 flex justify-between items-center">
      <a href="/" class="text-xl font-bold bg-linear-to-r from-blue-700 to-blue-500 bg-clip-text text-transparent tracking-tight">
        1120 PARK AVE
      </a>

      <div class="hidden md:flex space-x-8 font-medium text-slate-600">
        {navLinks.map((link) => (
          <a href={link.href} class="hover:text-blue-600 transition-colors">
            {link.name}
          </a>
        ))}
      </div>

      <a href="#help" class="bg-slate-900 text-white px-5 py-2 rounded-lg text-sm font-semibold hover:bg-slate-800 transition-all active:scale-95">
        Get Help
      </a>
    </div>
  </nav>
</header>